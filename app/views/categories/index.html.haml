.row
  .col-md-12
    %a{style: 'font-size:25px;', "aria-controls" => "collapseExample", "aria-expanded" => "false", "data-toggle" => "collapse", :href => "#collapseExample"}
      How To Play
      %i.fa.fa-sort-desc
  .col-md-12
    #collapseExample.collapse
      %br
      %p
        %strong LETTERLOOSE IS A GAME FOR PEOPLE WHO LIKE WORDS, WIT, AND LAUGHTER.
        %br
        The premise of the game is that you alter a film, book, or song title, or a common phrase, by adding, subtracting, or changing
        %strong ONE
        letter.  Now you've created a new title or phrase. The challenge is to construct a clever description for what you've just created.

      %p
        %strong For example:
        %ol
          %li Big Lebowski.
          %li Delete the 'g'.
          %li Bi Lebowski.
        %strong New description:
        Polish immigrant struggles with his sexuality
    .col-md-4.col-md-offset-4.form-group
      = link_to play_random_path, class: 'btn btn-primary btn-lg btn-block' do
        Random Play

.row
  .col-md-12
    .panel.panel-default
      .panel-body
        %h4.form-group Play by clicking on a category
        - @categories.each do |category|
          .category-link
            = link_to category.name, category_path(category)
      .panel-body
        %h4 Play by searching for a title
        = render partial: 'titles/search'

.row
  .col-md-4
    %h4.text-center
      HIGHEST RATED OF ALL TIME
    .panel.panel-default.highest-rated-container
      .panel-body
        - @highest_rated_descriptions_all_time.each do |description|
          %div{class: dom_id(description)}
            = render partial: 'shared/description', locals: {description: description}
          %hr

  .col-md-4
    %h4.text-center THIS WEEK'S HIGHEST RATED
    .panel.panel-default.weeks-highest-rated
      .panel-body
        - @current_weeks_highest_rated_descriptions.each do |description|
          %div{class: dom_id(description)}
            = render partial: 'shared/description', locals: {description: description}
          %hr

  .col-md-4
    %h4.text-center MOST RECENT
    .panel.panel-default
      .panel-body
        - @most_recent_descriptions.each do |description|
          %div{class: dom_id(description)}
            = render partial: 'shared/description', locals: {description: description}
          %hr

.row
  .col-md-12
    = link_to "SEE ALL DESCRIPTIONS", descriptions_path, class: 'btn btn-primary btn-block'

%br
%br

%br
